import {
  GraphData,
  getRangedAggregate,
  getUnitCount,
  graphDataToMostFrequent,
  graphToAverage,
} from "lib-fullstack/utils/aggregate";
import { db } from "lib-fullstack";
import { WPM_HIGH_THRESHOLD, WPM_LOW_THRESHOLD } from "lib-fullstack/utils/analyticThresholds";
import { roundToPrecision } from "lib-fullstack/utils/helperFunctions";

export type Badge = {
  title: string;
  copy: string;
  image: string;
  url?: string;
  urlCopy?: string;
};
